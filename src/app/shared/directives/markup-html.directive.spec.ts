import { MarkupHtmlDirective } from './markup-html.directive';

describe('MarkupHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkupHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
