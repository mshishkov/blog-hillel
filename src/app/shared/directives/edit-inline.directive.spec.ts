import { EditInlineDirective } from './edit-inline.directive';

describe('EditInlineDirective', () => {
  it('should create an instance', () => {
    const directive = new EditInlineDirective();
    expect(directive).toBeTruthy();
  });
});
